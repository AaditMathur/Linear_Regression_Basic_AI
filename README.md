# Linear_Regression_Basic_AI
A tutorial built basic linear regression trained model using scikit learn library

## Features
   -Streamlit UI
   -Simple scikit regression model
   -Interactive User Input
## How to run
```bash
pip install -r requirements.txt
streamlit run app.py
```